{
  "name": "venoxfans-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build && cd ../server && npm install && npm run build",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "start": "cd client && npm run start"
  },
  "dependencies": {
    "next": "15.1.7",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  }
}
